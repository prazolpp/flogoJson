{
  "name": "flogojson",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Prajwal Pyakurel",
  "license": "ISC",
  "dependencies": {
    "nunjucks": "^3.2.1"
  },
  "generator": {
    "supportedProtocols": [
      "http",
      "https",
      "ws",
      "wss",
      "kafka",
      "kafka-secure",
      "mqtt",
      "stomp"
    ],
    "generator": "<2.0.0",
    "parameters": {
      "server": {
        "description": "The server you want to use in the code.",
        "required": true
      },
      "resourceType": {
        "description": "Defines the type of resource. It can be either 'stream' or 'flow', at least at the moment.",
        "required": false
      }
    }
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}